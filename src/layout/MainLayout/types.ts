import { ReactNode } from 'react';

export const logo = './assets/logo/utech/utech-256x78.png';

export type MainLayoutProps = {
  pageContent: ReactNode;
};

export const currentSection = 1;

//Please replace this with a valid feedback link or mailto: address to pass into the feedbackLink prop of the UsiFooter component
export const feedbackLink = '';
